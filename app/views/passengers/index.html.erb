<h1>Rideshare Passengers</h1>
<div class = "index-list">
<ul>

<% @passengers.each do |passenger|%>
    <li><%=link_to "#{passenger.name}", passenger_path(passenger.id), method: :get %></li>
    <%=link_to "Edit", edit_passenger_path(passenger.id), method: :get, class: "link-list-edit" %>
    <%=link_to "Delete",passenger_path(passenger), method: :delete, confirm: "Are you sure?", class: "link-list-delete" %>
<% end %>
</ul>
</div>