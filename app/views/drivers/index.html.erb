<h1>Rideshare Drivers</h1>
<div>
<ul>

<% @drivers.each do |driver|%>
    <li><%=link_to "#{driver.name}", driver_path(driver.id), method: :get %>
   <%=link_to "Edit", edit_driver_path(driver.id), method: :get %></link_to>

<% end %>
</ul>
</div>